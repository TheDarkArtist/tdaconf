#!/bin/sh
set -eu

# traverse downward and obliterate all node_modules directories
# find . -type d -name node_modules -prune -exec rm -rf {} +
#

find . -type d \( -name node_modules -o -name .next \) -prune -exec rm -rf {} +

